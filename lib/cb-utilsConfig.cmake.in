@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# users of us require libgpiod
find_dependency(PkgConfig)
pkg_search_module(LIBGPIOD REQUIRED libgpiodcxx)

include("${CMAKE_CURRENT_LIST_DIR}/cb-utilsTargets.cmake")

check_required_components(cb-utils)
