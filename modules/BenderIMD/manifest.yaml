description: IMD module for Bender’s ISOMETER® isoCHA425HV
config:
  modbus_address:
    description: The device's Modbus address
    type: integer
    minimum: 1
    maximum: 247
    default: 3
provides:
  main:
    interface: isolation_monitor
    description: Main interface
requires:
  serial_comm_hub:
    interface: serial_communication_hub
metadata:
  license: https://spdx.org/licenses/Apache-2.0.html
  authors:
    - chargebyte GmbH
